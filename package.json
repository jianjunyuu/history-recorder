{
    "name": "@history-recorder/repo",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
      "lint": "eslint .",
      "lint:fix": "eslint . --fix",
      "test": "vitest --watch",
      "coverage": "vitest run --coverage",
      "prepare": "husky install"
    },
    "lint-staged": {
      "**/*": "eslint"
    },
    "devDependencies": {
      "@antfu/eslint-config": "^0.25.2",
      "@commitlint/cli": "^17.0.3",
      "@commitlint/config-conventional": "^17.0.3",
      "@vitejs/plugin-vue": "^3.0.0",
      "eslint": "^8.20.0",
      "typescript": "^4.6.4",
      "vitest": "^0.18.1"
    }
}
