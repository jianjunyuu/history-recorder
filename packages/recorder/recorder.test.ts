import { expect, it/* , test, vi ,describe */ } from 'vitest'
// import { createRecord } from '.'
// const recorder = useRecorder()
// recorder.forward()
// recorder.back()

// const { execute, revert } = createRecord()
// execute({ a: 1, b: 2 }, (state) => {
//   return Promise.resolve()
// })
// revert(null, (state) => {

// })

// describe('create an record', () => {
// const { execute, revert } = createRecord()
// const executeFn = vi.fn()
// execute({ a: 1, b: 2 }, (state) => {
//   executeFn()
//   return Promise.resolve()
// })

// expect(executeFn).toBeCalled()
// test('')
// })

it('is truly', () => {
  expect(0).toBe(0)
})
